services:
  - type: web
    name: bigdata-backend
    env: go
    rootDirectory: .
    buildCommand: "go build -tags netgo -ldflags='-s -w' -o app ./cmd/server"
    startCommand: "./app"
    envVars:
      - key: GO_VERSION
        value: 1.25.3
